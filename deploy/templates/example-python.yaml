apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: <APP_NAME>
  namespace: <NAMESPACE>
spec:
  template:
    spec:
      containers:
        - image: <DOCKER_REPO>
          env:
            - name: APP_PORT
              value: "5050"
          ports:
            - containerPort: 5050
          readinessProbe:
            httpGet:
              path: /
